# 用户端与康复师后台 UI 统一

## 问题与结果

之前的主题调整仅应用于康复师后台，用户端仍保留米色、深绿色与宋体标题。本次将用户端统一为后台的紫色柔光背景、薄荷绿辅助色、粗黑体标题、白色圆角卡片、轻边框与阴影。

覆盖首页、安全筛查、活动评估、起点推荐、今日训练、恢复路线、跟练课件、复盘、知识、安全与数据页，以及教学和课件弹窗。统一按钮、表单、侧栏与手机导航；红黄橙警示保留独立的状态颜色。手机首页先展示说明和开始入口，再展示辅助插图。

单文件离线版同步重新生成。训练逻辑、用户记录和后台上传发布流程未调整；本次发布基于已经修复上传进度和发布失败反馈的 94cecfa。

## 验证

- 静态与训练逻辑检查通过。
- 单文件构建一致性及启动检查通过。
- Chrome 无界面浏览器检查 1440px 桌面与 390px 手机：4 个引导步骤、5 个主视图以及安全、数据子页无横向溢出、无脚本异常。
- 复查首页、手机布局、恢复路线和课件页截图，修正浅色导航文字对比度与手机插图重叠。

## 发布

公开路径保持 https://bobjennies.click/spine-rehab/ ，后台保持 /spine-rehab/admin。采用独立版本目录切换，旧版本目录保留用于回滚，持久化课件数据和上传文件继续使用原目录。
